group 'com.jandadav'
version '1.0-SNAPSHOT'

task runIntegrationTest {
    dependsOn ':integration-test:test'
    group = 'verification'
    description = 'Run services and execute integration tests. Start the services with "npm run everuthing" first!'
}